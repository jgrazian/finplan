// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { AnalysisPoint } from "./AnalysisPoint";
import type { SweepAxis } from "./SweepAxis";
import type { SweepCell } from "./SweepCell";

/**
 * A finished sweep.
 */
export type SweepResults = {
  /**
   * One or two axes, in the order the cells' indices follow.
   */
  axes: Array<SweepAxis>;
  /**
   * Row-major over the axes: the last axis varies fastest.
   */
  cells: Array<SweepCell>;
  /**
   * The plan as it stands, run once, so every cell has something to be a
   * change from.
   */
  plan: AnalysisPoint;
  /**
   * Where the plan's own values sit on the axes, to the nearest step, or
   * `null` when they fall outside a swept range.
   */
  plan_indices: Array<number> | null;
  /**
   * Monte Carlo iterations behind each cell.
   */
  iterations: number;
};
