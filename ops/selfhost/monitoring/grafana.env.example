# /etc/grafana/finplan.env (root:grafana 0640). Read by grafana-server.service.
# Only applied when Grafana creates its database on first start; afterwards use
# `sudo grafana cli admin reset-admin-password <new>`.
GF_SECURITY_ADMIN_PASSWORD=change-me
